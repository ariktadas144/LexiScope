import type { Instruction } from '../../core/codegen/instruction'; export function IRView({ instructions }: { instructions: Instruction[] }) { return <div className="ir-list">{instructions.length ? instructions.map((instruction) => <div className="ir-row" key={instruction.index}><span>{String(instruction.index).padStart(2, '0')}</span><code>{instruction.result ? `${instruction.result} = ` : ''}{instruction.op} {instruction.arg1 ?? ''} {instruction.arg2 ?? ''}</code></div>) : <span className="muted">No IR generated</span>}</div> }
