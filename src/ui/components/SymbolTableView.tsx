import type { SymbolEntry } from '../../core/semantic/symbolTable'; export function SymbolTableView({ symbols }: { symbols: SymbolEntry[] }) { return <div className="symbol-table">{symbols.length ? symbols.map((symbol) => <div className="symbol-row" key={symbol.name}><b>{symbol.name}</b><span>{symbol.type}</span><span>{symbol.scope}</span></div>) : <span className="muted">No symbols yet</span>}</div> }
